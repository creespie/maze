[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "maze_lib"
version = "0.1.0"
description = "A maze generator library"
requires-python = ">=3.11.15"
authors = [
    { name = "Ludovico Rossi" },
    { name = "Lorenzo Mezzabarba" },
]